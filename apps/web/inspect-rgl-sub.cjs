try {
    const WidthProvider = require('react-grid-layout/build/WidthProvider');
    console.log('Subpath WidthProvider:', WidthProvider);
} catch (e) {
    console.log('Subpath WidthProvider failed:', e.message);
}
