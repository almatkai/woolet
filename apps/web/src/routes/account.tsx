
export function AccountPage() {
    return (
        <div className="flex items-center justify-center h-full">
            <p className="text-muted-foreground">Account settings are managed through the sidebar menu.</p>
        </div>
    );
}
