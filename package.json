{
  "name": "woolet",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "dev:work3": "PORT=3007 WEB_PORT=3001 LANDING_PORT=4322 WEB_URL=http://localhost:3001 API_URL=http://localhost:3007 turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "db:generate": "turbo run db:generate --filter=@woolet/api",
    "db:migrate": "turbo run db:migrate --filter=@woolet/api",
    "db:push": "turbo run db:push --filter=@woolet/api"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "^5.7.2"
  },
  "resolutions": {
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0"
  },
  "packageManager": "bun@1.3.8",
  "engines": {
    "node": ">=20"
  }
}